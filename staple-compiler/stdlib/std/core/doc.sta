pub mod

use std.syntax.(Expr, Item, Parenthesized)

/// Attaches documentation text to an item.
///
/// # Example
///
/// ```staple
/// @doc("Returns the configured port.")
/// pub def port = () => 8080
/// ```
pub macro @doc: Parenthesized (Expr) -> Item -> Item
