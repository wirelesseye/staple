use std.cinterop *

extern "c" {
    let printf: (CPointer CChar, ...) -> I32
}

pub def print: String -> () = value => {
    let _ = printf (c_string "%s", string_to_c_string value)
    ()
}

pub def println: String -> () = value => {
    let _ = printf (c_string "%s\n", string_to_c_string value)
    ()
}
