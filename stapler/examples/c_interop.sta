use std.cinterop.*

extern "c" {
    let printf: (CPointer CChar, ...) -> I32
}

printf (c_string "hello, world!\n")
printf (CString.from_string "owned Staple String\n")
let round_trip = CString.to_string (c_string "CString round trip\n")
printf (CString.from_string round_trip)
