// Namespace, selected, renamed, and glob imports.
use sample_modules.geometry
use sample_modules.geometry (Point, origin)
use sample_modules.geometry translate as move
use sample_modules.messages *
use std.io println

let start: Point = origin ()
let finish = move (start, 20, 22)

let start_coordinates = (geometry.x start, geometry.y start)
let finish_coordinates = (geometry.x finish, geometry.y finish)

def main = () => {
    println title
    println "Namespace, selected, renamed, and glob imports evaluated successfully."
}
